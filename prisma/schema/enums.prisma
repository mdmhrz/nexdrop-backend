enum UserRole {
  SUPER_ADMIN
  ADMIN
  RIDER
  CUSTOMER
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum ParcelStatus {
  REQUESTED
  ASSIGNED
  PICKED
  IN_TRANSIT
  DELIVERED
  CANCELLED
}

enum RiderStatus {
  AVAILABLE
  BUSY
  OFFLINE
}

enum RiderAccountStatus {
  PENDING
  ACTIVE
  SUSPENDED
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
}

enum PaymentMethod {
  STRIPE
  MANUAL
  BKASH
  SSLCOMMERZ
}

enum EarningStatus {
  PENDING
  PAID
}

enum CashoutStatus {
  PENDING
  APPROVED
  REJECTED
  PAID
}

enum ParcelType {
  DOCUMENT
  SMALL
  MEDIUM
  LARGE
  FRAGILE
  ELECTRONICS
}

enum ServiceType {
  STANDARD
  EXPRESS
}

enum NotificationType {
  INFO
  WARNING
  SUCCESS
}
